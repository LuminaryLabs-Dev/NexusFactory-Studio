{
  "name": "@luminarylabs/nexusfactory-studio",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Registry-driven visual host for NexusFactory kits.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.js && node --check src/registry-host.js && node --check src/runtime-host.js && node --check src/editor-contract.js && node --check src/viewers/mesh-3d.js",
    "validate": "npm run check && npm test",
    "serve": "node scripts/serve.mjs"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
